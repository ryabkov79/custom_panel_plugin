<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function custom_panel_plugin_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'ctools') {
    return 'plugins/' . $plugin_type;
  }
}